# Exact pins for reproducible builds - see requirements.txt.
# torch/torchvision: "==x.y.z" also matches local builds like 2.9.1+rocm6.4,
# so the same pin works for the CUDA (PyPI) and ROCm (TORCH_INDEX_URL) path.
torch==2.9.1
torchvision==0.24.1
# Florence-2's remote code breaks on the 4.45 cache/attention-mask refactor
# (>=4.46: attention-mask size mismatch; >=4.47: missing forced_bos_token_id)
transformers==4.44.2
einops==0.8.2
timm==1.0.29
facenet-pytorch==2.5.3
