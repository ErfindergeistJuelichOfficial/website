# Exact pins for reproducible builds - a rebuild months later must not pull
# breaking new majors (like OpenCV 5, which removed cv2.CascadeClassifier).
# Upgrading is a deliberate step: bump a pin, rebuild, test process/upload.
pillow==12.3.0
opencv-python-headless==4.14.0.94
numpy==2.5.2
piexif==1.1.3
